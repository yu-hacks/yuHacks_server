import { model } from 'mongoose';
export function typedModel(name, schema, collection, skipInit, statics, connection) {
    if (schema && statics)
        schema.statics = statics;
    if (connection)
        return connection.model(name, schema, collection);
    return model(name, schema, collection, skipInit);
}
