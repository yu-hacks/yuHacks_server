import { Schema, Document, Model, Connection } from 'mongoose';
import { Extract } from './types';
export declare function typedModel<T extends Schema, S extends {
    [name: string]: Function;
}>(name: string, schema?: T, collection?: string, skipInit?: boolean, statics?: S & ThisType<Model<Document & Extract<T>>>, connection?: Connection): Model<Document & Extract<T>> & S;
