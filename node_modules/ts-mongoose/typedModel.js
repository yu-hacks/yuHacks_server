"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var mongoose_1 = require("mongoose");
function typedModel(name, schema, collection, skipInit, statics, connection) {
    if (schema && statics)
        schema.statics = statics;
    if (connection)
        return connection.model(name, schema, collection);
    return mongoose_1.model(name, schema, collection, skipInit);
}
exports.typedModel = typedModel;
