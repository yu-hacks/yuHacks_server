import { SchemaOptions, Schema } from 'mongoose';
import { GetSchemaType } from './types';
declare type CreateSchema = <T extends {
    [x: string]: any;
}, O extends SchemaOptions>(definition?: T, options?: O) => Schema & {
    definition: {
        [P in keyof GetSchemaType<O, T>]: GetSchemaType<O, T>[P];
    };
    options: O;
};
export declare const createSchema: CreateSchema;
export {};
